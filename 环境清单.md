# Total-pipe 工作流环境清单

> 目标：把「论文 PDF → 研究汇报 PPT」这条链路跑起来。
> 链路：paperworkflow → pwf2rpa → research_ppt → 渲染(slidep) → pptx-telemetry 校验。
> 标注说明：`[本机实测]` 是当前机器已配好的值；`[换机通用]` 是迁移到新机器时的最低要求。

---

## 一、运行时（3 个）

| 组件 | 用途 | 版本要求 | 本机实测路径 |
|---|---|---|---|
| **Node.js 24** | research_ppt MCP server + RPA CLI + pptx-telemetry 调 RPA QA | **必须 24**（RPA 硬性要求，README 推荐 24，CI 覆盖 22/24） | `D:/Node24/node.exe`（v24.20.0 Krypton LTS，npm 11.19.0） |
| **Python（paperworkflow 专用）** | PDF→markdown 证据流水线 | ≥3.12，用 uv 管理依赖 | `F:/Workbuddy/Total-pipe/paperworkflow/.venv/Scripts/python.exe`（3.12.10，uv 0.9.26） |
| **Python（pwf2rpa + pptx-telemetry）** | 容量门禁 / OOXML 遥测 | pwf2rpa 纯标准库（任意 3.11+）；pptx-telemetry 需 **Pillow** | pwf2rpa 用 `~/.workbuddy/binaries/python/versions/3.13.12/python.exe`；pptx-telemetry 用 `~/.workbuddy/binaries/python/envs/default/Scripts/python.exe`（Pillow 12.3.0） |

### 依赖明细

- **paperworkflow**（pyproject.toml，用 `uv sync` 装）：`loguru` `numpy` `openai` `pyyaml` `requests` `tokenizers` `tqdm`；另需 **MinerU 官方 CLI**（`mineru-open-api`，装在 paperworkflow venv 内，`[本机实测]` 已装）。
- **pwf2rpa**：零第三方 Python 依赖（纯标准库）。`refresh.py` 内嵌的 Node 脚本需要 Node 24（用于 `pwf2rpa_refresh_capacity`）。
- **pptx-telemetry**：仅 `Pillow`（pptx_sidecars.py 裁派生图算 SHA + 字体度量）。
- **research-ppt-assistant**：零 npm 依赖（node_modules 为空，`[本机实测]`）。

---

## 二、外部服务 / API（2 个）

| 服务 | 必需性 | 作用 | 配置位置 | 本机状态 |
|---|---|---|---|---|
| **MinerU** | **必需** | PDF 转 Markdown + 扫描页 OCR（PDF→证据的唯一入口） | `paperworkflow/config.yaml` → `api.mineru`，或环境变量 `MINERU_TOKEN` | `[已配]` mode=official_cli，api_key 已填 |
| **LLM（OpenAI 兼容）** | 可选 | 长文总结/问答增强 | `paperworkflow/config.yaml` → `api.llm`（api_key / base_url / model_name） | `[未配]` 三项为空；DeepSeek / Moonshot 等按需填 |

> MinerU 是硬依赖：没它 PDF 根本进不了流水线。LLM 是软依赖，literature_workflow 主要靠 MinerU OCR + 本地证据处理就能产出 workflow.json。

---

## 三、MCP 连接器（3 个，全部要在连接器管理页点 Trust）

配置在 `~/.workbuddy/mcp.json`，换机后要改三处绝对路径：

| 连接器 | command | args | cwd |
|---|---|---|---|
| `research_ppt` | `D:/Node24/node.exe` | `C:/Users/Beibei/plugins/research-ppt-assistant/server/mcp-server.mjs` | `C:/Users/Beibei/plugins/research-ppt-assistant` |
| `paperworkflow` | `F:/Workbuddy/Total-pipe/paperworkflow/.venv/Scripts/python.exe` | `F:/Workbuddy/Total-pipe/paperworkflow/integrations/mcp/server.py` | `F:/Workbuddy/Total-pipe/paperworkflow` |
| `pwf2rpa` | `C:/Users/Beibei/.workbuddy/binaries/python/versions/3.13.12/python.exe` | `F:/Workbuddy/pwf2rpa/integrations/mcp/server.py` | `F:/Workbuddy/pwf2rpa` |

> 注意：RPA 的 node 路径**不要**用 WorkBuddy managed 的 `binaries/node/versions/<ver>/node.exe`（升级会漂移目录导致 MCP 断连），用独立固定路径 `D:/Node24/node.exe`。
> pptx-telemetry 不是 MCP，是 skill 里的脚本（见下）。

---

## 四、Skill（符号链接，WorkBuddy 从 `~/.workbuddy/skills/` 加载）

| Skill | 链接 | 真身 |
|---|---|---|
| `total-pipe-deck` | `C:/Users/Beibei/.workbuddy/skills/total-pipe-deck` | `F:/Workbuddy/Total-pipe/skills/total-pipe-deck` |
| `pptx-telemetry` | `C:/Users/Beibei/.workbuddy/skills/pptx-telemetry` | `F:/Workbuddy/Total-pipe/skills/pptx-telemetry` |
| `tencent-pptx`（渲染用，内置） | — | WorkBuddy 内置 plugin |

换机重建链接（PowerShell 管理员）：
```powershell
New-Item -ItemType SymbolicLink -Path "C:\Users\<你>\.workbuddy\skills\pptx-telemetry" -Target "F:\Workbuddy\Total-pipe\skills\pptx-telemetry"
New-Item -ItemType SymbolicLink -Path "C:\Users\<你>\.workbuddy\skills\total-pipe-deck" -Target "F:\Workbuddy\Total-pipe\skills\total-pipe-deck"
```

---

## 五、代码仓库 / 符号链接

| 组件 | 路径 | 说明 |
|---|---|---|
| research-ppt-assistant（RPA） | `C:/Users/Beibei/plugins/research-ppt-assistant` | symlink → `F:/Project/PPTcreator/plugins/research-ppt-assistant`，v0.6.1 |
| pwf2rpa | `F:/Workbuddy/pwf2rpa` | symlink → `F:/Workbuddy/Total-pipe/pwf2rpa` |
| Total-pipe 聚合仓库 | `F:/Workbuddy/Total-pipe` | 纳管 pwf2rpa / skills / Team编排；paperworkflow 与 Test* 已在 .gitignore |

---

## 六、渲染器

- **tencent-pptx** skill（引擎即 slidep），产出 `.pptx` + `slides/*.slide`。
- 渲染后 shape 级校验走 **pptx-telemetry**（pptx_sidecars.py 解析 OOXML → run_qa.py 接 RPA QA），不依赖渲染器导出遥测。

---

## 七、字体（pptx-telemetry 真实度量用）

- 微软雅黑 `C:/Windows/Fonts/msyh.ttc` + `msyhbd.ttc`（`[本机实测]` 均已存在）。
- 换机（尤其精简版系统/非中文 Windows）需确认这两文件在，否则 sidecar 生成挂。

---

## 八、自检命令（逐条过一遍即证明环境就绪）

```bash
# 1. Node 24
D:/Node24/node.exe --version          # 期望 v24.20.0

# 2. RPA MCP 握手（应返回 serverInfo v0.6.1 + 18 个工具）
cd C:/Users/Beibei/plugins/research-ppt-assistant
printf '%s\n' \
 '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-06-18","capabilities":{},"clientInfo":{"name":"p","version":"0"}}}' \
 '{"jsonrpc":"2.0","id":2,"method":"tools/list","params":{}}' \
 | D:/Node24/node.exe server/mcp-server.mjs

# 3. Pillow（pptx-telemetry）
C:/Users/Beibei/.workbuddy/binaries/python/envs/default/Scripts/python.exe -c "import PIL; print(PIL.__version__)"

# 4. paperworkflow venv + MinerU CLI
F:/Workbuddy/Total-pipe/paperworkflow/.venv/Scripts/python.exe --version   # 3.12.x
ls F:/Workbuddy/Total-pipe/paperworkflow/.venv/Scripts/mineru-open-api.exe

# 5. pwf2rpa 干跑（应返回 slide_count / warnings，不落盘）
C:/Users/Beibei/.workbuddy/binaries/python/versions/3.13.12/python.exe \
  -c "import pwf2rpa; print('pwf2rpa ok')"
```

---

## 九、最容易踩的坑（按优先级）

1. **RPA node 路径漂移**：mcp.json 里必须写 `D:/Node24/node.exe`，别写 WorkBuddy managed 的 `versions/<ver>/node.exe`。
2. **MCP 未 Trust**：三个连接器没点 Trust，工具直接不存在——先 Trust 再排查代码。
3. **MinerU 没配/没 token**：PDF 进不了流水线，`synthesis_readiness` 过不了，别误判成 OCR 问题。
4. **paperworkflow 路径沙箱**：outline/search_evidence 只收 `.md`、process_pdf 只收 `.pdf`，且路径必须落在 paperworkflow 项目目录内。
5. **缺字体/Pillow**：pptx-telemetry 的 sidecar 生成直接挂。
